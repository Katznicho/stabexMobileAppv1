import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const SelfRegistration = ({ cardApplication, setCardApplication, goToNextStep, errors, setErrors, regions , stations }: any) => {
    return (
        <View>
            <Text>SelfRegistration</Text>
        </View>
    )
}

export default SelfRegistration

const styles = StyleSheet.create({})